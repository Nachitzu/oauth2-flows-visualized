import { Vulnerability } from '@/domain/types';

export const vulnerabilities: Record<string, Vulnerability> = {
  'open-redirect': {
    id: 'open-redirect',
    cweId: 'CWE-601',
    severity: 'critical',
    title: 'Open Redirect via URI Manipulation',
    description: 'The authorization server does not validate the redirect_uri parameter exactly. An attacker can manipulate the redirect URI to point to a malicious site under their control, stealing the authorization code.',
    proofOfConcept: 'Attacker crafts a link: /authorize?redirect_uri=https://legitimate.com&redirect_uri=https://evil.com. If the server only validates that redirect_uri starts with the registered base, the second URI overrides the first.',
    mitigation: 'Validate redirect_uri using exact string matching against the registered URI. Never allow redirects to external domains not in the allowlist.',
    rfcRef: 'RFC 6749 §4.1.2',
    references: [
      'https://portswigger.net/web-security/oauth',
      'https://www.oauth.com/oauth2-servers/authorization/security-considerations/',
    ],
  },
  'state-omission': {
    id: 'state-omission',
    cweId: 'CWE-352',
    severity: 'critical',
    title: 'CSRF Attack via Missing state Parameter',
    description: 'Without the state parameter, an attacker can initiate an authorization flow in the user\'s browser and redirect the authorization code to their own endpoint, hijacking the user\'s session.',
    proofOfConcept: 'Attacker lures victim to click a link. The authorization server redirects with the code to the attacker\'s registered redirect_uri. Attacker exchanges the code for tokens.',
    mitigation: 'Always use and validate the state parameter. Generate a cryptographically random state value, store it in a session cookie, and validate it upon callback.',
    rfcRef: 'RFC 6749 §4.4.2',
    references: [
      'https://datatracker.ietf.org/doc/rfc9700/',
    ],
  },
  'code-interception': {
    id: 'code-interception',
    cweId: 'CWE-345',
    severity: 'critical',
    title: 'Authorization Code Interception',
    description: 'The authorization code is transmitted via URL query string, exposing it in browser history, server logs, and HTTP referrer headers.',
    proofOfConcept: 'Code appears in URLs like /callback?code=AUTH_CODE_123. Any local/network attacker can read it from the URL.',
    mitigation: 'Use PKCE for public clients. For confidential clients, ensure short-lived codes and use POST for exchange. Transport must be HTTPS only.',
    rfcRef: 'RFC 6749 §4.1.2',
    references: [
      'https://oauth.net/2/pkce/',
      'https://www.oauth.com/oauth2-servers/authorization/security-considerations/',
    ],
  },
  'plain-code-challenge-method': {
    id: 'plain-code-challenge-method',
    cweId: 'CWE-345',
    severity: 'critical',
    title: 'Plain Text code_challenge Method',
    description: 'Using code_challenge_method=plain means the code_verifier is sent in plain text. If intercepted, attackers can use it to exchange a stolen authorization code.',
    proofOfConcept: 'Attacker intercepts authorization code from URL. If code_challenge_method=plain, the attacker also gets the verifier needed to exchange the code.',
    mitigation: 'Always use S256 (SHA-256) as the code_challenge_method. This ensures that even if the code is intercepted, it cannot be exchanged without the original verifier.',
    rfcRef: 'RFC 7636 §4.6',
    references: [
      'https://auth0.com/docs/get-started/authentication-and-authorization-flow/authorization-code-flow-with-pkce',
    ],
  },
  'code-verifier-guessable': {
    id: 'code-verifier-guessable',
    cweId: 'CWE-341',
    severity: 'critical',
    title: 'Predictable code_verifier',
    description: 'If the code_verifier is not cryptographically random (min 43 chars, high entropy), an attacker can brute-force or guess it and exchange a stolen code.',
    proofOfConcept: 'Client uses a short or predictable code_verifier like "password123". Attacker with the code can quickly brute-force the verifier.',
    mitigation: 'Generate code_verifier using a cryptographically secure random generator. Minimum 43 characters from the base64url alphabet (RFC 7636 §4.1)',
    rfcRef: 'RFC 7636 §4.1',
    references: [
      'https://oauth.net/2/pkce/',
    ],
  },
  'redirect-uri-mismatch': {
    id: 'redirect-uri-mismatch',
    cweId: 'CWE-346',
    severity: 'critical',
    title: 'Token Hijacking via redirect_uri Mismatch',
    description: 'The authorization server does not validate that the redirect_uri used in the callback matches the one used in the authorization request. Tokens are sent to the wrong URI.',
    proofOfConcept: 'Attacker registers a malicious redirect_uri with the client app. When the authorization code is issued, the server sends the tokens to the attacker\'s URI.',
    mitigation: 'The authorization server MUST validate that the redirect_uri in the callback exactly matches the one in the authorization request.',
    rfcRef: 'RFC 6749 §4.1.2',
    references: [
      'https://www.oauth.com/oauth2-servers/authorization/security-considerations/',
    ],
  },
  'token-leakage-via-referrer': {
    id: 'token-leakage-via-referrer',
    cweId: 'CWE-201',
    severity: 'risk',
    title: 'Token Leakage via HTTP Referrer',
    description: 'When the client makes a request to the resource server, the access token may be sent as a query parameter or header. If the page has external links, the referrer header can leak the token.',
    proofOfConcept: 'Client page has <a href="https://external.com">. When user clicks it, the browser sends a Referrer header containing the full URL with the access token to external.com.',
    mitigation: 'Use Authorization header (Bearer token) instead of query parameters. Add rel="noopener" to external links. Use CSP headers to control referrer behavior.',
    rfcRef: 'RFC 6749 §2.3',
    references: [
      'https://portswigger.net/web-security/oauth',
    ],
  },
  'client-secret-exposure': {
    id: 'client-secret-exposure',
    cweId: 'CWE-798',
    severity: 'critical',
    title: 'Hardcoded client_secret in Frontend Code',
    description: 'The client_secret is embedded in frontend JavaScript code, making it fully visible to anyone who inspects the source.',
    proofOfConcept: 'Attacker opens DevTools and reads: const client_secret = "super_secret_123". Uses it to obtain tokens directly from the token endpoint.',
    mitigation: 'For public clients (SPAs, mobile apps), NEVER use client_secret. Use PKCE instead. For confidential clients, keep secrets server-side only.',
    rfcRef: 'RFC 6749 §2.3.1',
    references: [
      'https://www.oauth.com/oauth2-servers/authorization/security-considerations/',
    ],
  },
  'token-not-rotated': {
    id: 'token-not-rotated',
    cweId: 'CWE-320',
    severity: 'risk',
    title: 'Refresh Token Not Rotated',
    description: 'The refresh token is reused multiple times without rotation. If a refresh token is stolen, an attacker can use it indefinitely to obtain new access tokens.',
    proofOfConcept: 'Attacker steals refresh token from a compromised device. Since the server does not rotate tokens, the attacker can keep using the same refresh token.',
    mitigation: 'Implement refresh token rotation. Each time a refresh token is used, issue a new refresh token and invalidate the old one (RFC 6749 §6)',
    rfcRef: 'RFC 6749 §6',
    references: [
      'https://www.oauth.com/oauth2-servers/authorization/security-considerations/',
    ],
  },
  'dns-spoofing': {
    id: 'dns-spoofing',
    cweId: 'CWE-293',
    severity: 'risk',
    title: 'DNS Spoofing on HTTP Redirect',
    description: 'The authorization request or redirect uses HTTP instead of HTTPS. An attacker on the network can spoof DNS and redirect the authorization code or tokens to their own server.',
    proofOfConcept: 'User is on public WiFi. Attacker runs DNS spoofing to point the authorization server domain to their IP. All traffic, including the redirect with the auth code, goes through the attacker.',
    mitigation: 'ALWAYS use HTTPS for all OAuth communications. The authorization server MUST require HTTPS. HSTS should be enabled.',
    rfcRef: 'RFC 6749 §4.4.1',
    references: [
      'https://datatracker.ietf.org/doc/rfc9700/',
    ],
  },
  'iframe-clickjacking': {
    id: 'iframe-clickjacking',
    cweId: 'CWE-1021',
    severity: 'risk',
    title: 'Clickjacking on Authorization Endpoint',
    description: 'The authorization page is rendered in an iframe, allowing an attacker to overlay it with invisible elements and trick the user into performing actions (e.g., granting access).',
    proofOfConcept: 'Attacker embeds the auth page in an iframe on their malicious site, with transparent buttons overlaying the "Allow" button to automatically grant consent.',
    mitigation: 'Set X-Frame-Options: DENY or Content-Security-Policy: frame-ancestors none on the authorization server. This prevents the page from being embedded.',
    rfcRef: 'RFC 8252 §8.3',
    references: [
      'https://portswigger.net/web-security/oauth',
    ],
  },
  'token-in-query-string': {
    id: 'token-in-query-string',
    cweId: 'CWE-598',
    severity: 'risk',
    title: 'Access Token in Query String',
    description: 'The access token is transmitted via URL query parameter instead of the Authorization header, exposing it in logs, browser history, and referrer headers.',
    proofOfConcept: 'GET /api/resource?access_token=eyJhbGciOiJSUzI1NiIsInR5cCI... — Token visible in logs, browser history, and shared via URL.',
    mitigation: 'Always send access tokens in the Authorization header: Authorization: Bearer <token>. This keeps tokens out of URLs and server logs.',
    rfcRef: 'RFC 6749 §2.3',
    references: [
      'https://www.oauth.com/oauth2-servers/authorization/security-considerations/',
    ],
  },
};